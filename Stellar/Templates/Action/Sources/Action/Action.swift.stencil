//  {{ name }}.swift

public struct {{ name }} {
    
    private var argument: String
    
    public init(argument: String) {
        self.argument = argument
    }
    
    public func call() {
        print("{{ name }} called with argument 'argument' of value '\(argument)'.")
    }
}
